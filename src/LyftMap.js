import React, {Component} from 'react';
import {
	BrowserRouter,
	Route,
	Switch,
	NavLink,
	Redirect
} from 'react-router-dom'

import { withGoogleMap, GoogleMap, Marker, InfoWindow } from 'react-google-maps'

const LyftMap = ({model}) => {
	return (<div> LyftMap </div>);
}


export default LyftMap;